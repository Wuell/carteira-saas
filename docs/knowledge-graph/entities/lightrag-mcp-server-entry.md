---
entity: "LightRAG MCP Server Entry"
entity_type: data
community: 38
degree: 5
---

# LightRAG MCP Server Entry

**Type:** data  
**Community:** [[community-38]]  
**Degree:** 5

## Description

- A specific configuration block within `mcpServers Configuration Key` defining how to run the LightRAG server.

## Related

- [[entities/kg-server-command|KG Server Command]] — The `LightRAG MCP Server Entry` specifies the `KG Server Command` as part of its arguments.
- [[entities/uv-tool|Uv Tool]] — The `LightRAG MCP Server Entry` specifies the `Uv Tool` as its command.
- [[entities/mcpservers-configuration-key|mcpServers Configuration Key]] — The `mcpServers Configuration Key` should include the `LightRAG MCP Server Entry`.
- [[entities/absolute-path-resolution|Absolute Path Resolution]] — The `LightRAG MCP Server Entry` requires `Absolute Path Resolution` for its command arguments.
- [[entities/mcp-check-command|MCP Check Command]] — The `MCP Check Command` validates the `LightRAG MCP Server Entry` within the MCP configuration.

## Appears in

- `RAG-INIT.md`
