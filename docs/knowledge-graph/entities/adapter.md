---
entity: "Adapter"
entity_type: concept
community: 89
degree: 4
---

# Adapter

**Type:** concept  
**Community:** [[community-89]]  
**Degree:** 4

## Description

- An instance of `PrismaNeonHttp` used to configure the `PrismaClient` for database connection.

## Related

- [[entities/prismaclient|PrismaClient]] — The `PrismaClient` is configured to use the `Adapter`.
- [[entities/prismaneonhttp|PrismaNeonHttp]] — The `Adapter` is an instance of `PrismaNeonHttp`.
- [[entities/createprismaclient|CreatePrismaClient]] — The `createPrismaClient` function creates an `Adapter` instance.
- [[entities/database-url|Database Url]] — The `Adapter` uses the `Database Url` for connection.

## Appears in

- `lib/db.ts`
