---
entity: "Brapi API"
entity_type: organization
community: 88
degree: 4
---

# Brapi API

**Type:** organization  
**Community:** [[community-88]]  
**Degree:** 4

## Description

- An external API (`brapi.dev`) used for fetching quotes of Brazilian stocks and FIIs.

## Related

- [[entities/detecttickertype|detectTickerType]] — The `detectTickerType` function interacts with the `Brapi API` to fetch stock data.
- [[entities/brapi_token|BRAPI_TOKEN]] — The `BRAPI_TOKEN` is used for authentication when interacting with the `Brapi API`.
- [[entities/getbatchstockquotes|getBatchStockQuotes]] — The `getBatchStockQuotes` function queries the `Brapi API` for stock prices.
- [[entities/brapi-quote-api-endpoint|Brapi Quote API Endpoint]] — The `Brapi API` provides access via the `Brapi Quote API Endpoint`.

## Appears in

- `lib/quotes.ts`
